<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow emulator localhost and the dev server host for cleartext (HTTP) during development -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">192.168.45.101</domain>
    </domain-config>

    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">k13s406.p.ssafy.io</domain>
    </domain-config>

    <!-- Fallback: allow cleartext for all (only if you understand the security implications) -->
    <!--
    <base-config cleartextTrafficPermitted="true" />
    -->
</network-security-config>